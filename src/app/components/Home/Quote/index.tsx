import { Typography } from "@mui/material";

const Quote = () => {
  return (
    <section className="p-[32px] sm:p-[100px] md:p-[200px] border-b-2 border-black ">
      <div className="text-center">
        <Typography variant="h2">
          Stickers are like a temporary tattoo
        </Typography>
        <Typography variant="h2">for your stuff, </Typography>
        <Typography variant="h2">and we can’t get enough</Typography>
      </div>
    </section>
  );
};

export default Quote;
